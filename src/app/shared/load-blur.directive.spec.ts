import { LoadBlurDirective } from './load-blur.directive';

describe('LoadBlurDirective', () => {
  it('should create an instance', () => {
    const directive = new LoadBlurDirective();
    expect(directive).toBeTruthy();
  });
});
